# 总设计收敛条款（V1.9 历史存档）

> 来源：`DESIGN.md` §2 的历史版本（2026-09-19 与 DeepSeek 总设计对话的 V1.9 收敛稿），因总纲瘦身移至本存档。
> 说明：原文逐条保留，不作修改；现行归宿见下表——**原则级**已并入 `DESIGN.md` §2.2，**细则级**均已落入对应 SPEC（经核对无孤儿条款）。
> 维护：本存档不再更新；现行有效设计以 `DESIGN.md` 与各 SPEC 为准。

## 一、原条款（34 条）

1. 项目独立。
2. 聊天通道与内容生成解耦，通道为插件式适配器。
3. 世界包文件可修改，仅保留最新版；世界实例创建后设定完全锁死。
4. AI 生成，用户审定。
5. 用户在世界外。
6. 角色与世界高耦合。
7. 认知软约束为默认。
8. 时间同步是基础设施；世界时钟与现实时间存在可调倍率关系。
9. 世界设定初始确定，后续修改通过引入世界事件 + 创建时间线进行。
10. 版本管理作用于世界运行状态层面。
11. 仅激活时间线推进；允许同时激活多条时间线。
12. 角色性格由性格单元构成，置信度控制生成、变化、消失。
13. 性格单元有四种驱动方式：事件、对话、时间、锚点。
14. 锚点驱动是性格单元的一种驱动方式，不再有独立的锚点模型。
15. 对话上下文随时间线隔离。
16. 通道插件开放第三方开发，第三方插件作为独立进程运行（仅桌面端）。
17. 披露撤回通过时间线回滚实现。
18. 内建聊天窗口作为核心进程的一部分，遵循统一消息协议；拆分后仅保留插件版本。
19. 世界实例内部数据完全黑箱；世界时钟作为公开信息展示，仅展示当前激活时间线的时间。
20. 支持世界实例导入导出；导出包不加密，接受用户可能查看内部数据。
21. 安卓端不开放插件；安卓端支持世界实例创建、对话与世界包文件修改；支持表单式与对话式两种方式。
22. 项目不提供云同步服务，所有跨端同步由用户手动完成。
23. 驱动迁移隐式存在，不显式记录，不对用户可见。
24. 角色卡初始支持用户自定义或 AI 根据描述生成，AI 生成后需用户检查，仅保留最终确认版本。
25. 导入始终创建新世界实例，不修改或覆盖任何已有实例。
26. 内建聊天窗口拆分后，安卓端始终使用内建聊天实现，与桌面端插件版本并行；安卓端内建实现遵循统一消息协议；保持独立。
27. 不支持世界合并。
28. 世界时钟倍率在每一自然整秒时更新生效；倍率归属以用户输入时刻为准；若输入时刻恰为整秒，归属下一个整秒。
29. 安卓端与桌面端共享世界包生成器；当前版本生成的世界包保证在当前版本可运行；两端版本不同时按各自版本规则处理；跨大版本不兼容时先找转换器，无转换器则提示使用对应兼容版本运行。
30. 导出包包含世界包名称信息；导入实例名称基于此自动生成。
31. 倍率上限可扩展；默认上限 2592000；全局配置；仅开发者可配置。
32. 世界包名称嵌入世界包快照中；系统以原始名称为准，用户修改不生效。
33. 全局不允许同名世界实例，不区分世界。
34. 用户对导出世界包文件的修改，项目不提供任何支持，后果由用户自行承担；已创建的世界实例与世界包文件无关联。

## 二、逐条归宿

| 条款 | 现行归宿 |
|---|---|
| 1 项目独立 | `DESIGN.md` §2.2-1（原则） |
| 2 通道解耦 | `DESIGN.md` §2.2-12（原则）；细则 `CHANNEL_PLUGIN_SPEC.md` |
| 3 世界包可修改 + 实例锁死 | `DESIGN.md` §2.2-7（原则）；细则 `WORLD_SETTING_SPEC.md` |
| 4 AI 生成，用户审定 | `DESIGN.md` §2.2-10（原则）；流程 `WORLD_SETTING_SPEC.md`、`CHARACTER_CARD_SPEC.md` |
| 5 用户在世界外 | `DESIGN.md` §2.2-3（原则） |
| 6 角色与世界高耦合 | `DESIGN.md` §2.2-2（原则） |
| 7 认知软约束 | `DESIGN.md` §2.2-5（原则）；细则 `CHARACTER_CARD_SPEC.md`、`WORLD_RUNTIME_SPEC.md` |
| 8 时间同步/倍率 | `DESIGN.md` §2.2-4（原则）；细则 `WORLD_RUNTIME_SPEC.md` |
| 9 修改走事件+新时间线 | `DESIGN.md` §2.2-8（原则）；细则 `EVENT_ENGINE_SPEC.md` §八 |
| 10 版本管理作用域 | `DESIGN.md` §2.2-8（原则）；细则 `WORLD_RUNTIME_SPEC.md` |
| 11 多时间线同时激活 | `WORLD_RUNTIME_SPEC.md` |
| 12–14 性格单元 / 四驱动 / 锚点 | `DESIGN.md` §2.2-11（原则）；细则 `WORLD_RUNTIME_SPEC.md`、`CHARACTER_CARD_SPEC.md` |
| 15 对话上下文隔离 | `SESSION_CORE_SPEC.md` |
| 16 第三方插件独立进程 | `DESIGN.md` §2.2-12（原则）；细则 `CHANNEL_PLUGIN_SPEC.md` |
| 17 披露撤回 = 回滚 | `SESSION_CORE_SPEC.md`、`WORLD_RUNTIME_SPEC.md` |
| 18 内建聊天归属与拆分 | `CHANNEL_PLUGIN_SPEC.md` §四；`DESKTOP_SPEC.md` §一 |
| 19 黑箱 + 时钟公开 | `DESIGN.md` §2.2-9（原则）；细则 `WORLD_SETTING_SPEC.md`、`DESKTOP_SPEC.md` |
| 20 导入导出不加密 | `WORLD_SETTING_SPEC.md` |
| 21 安卓端范围 | `ANDROID_SPEC.md` |
| 22 无云同步 | `DESIGN.md` §2.2-13（原则）；细则 `WORLD_SETTING_SPEC.md` |
| 23 驱动迁移隐式 | `DESIGN.md` §2.2-11（原则）；细节 `WORLD_RUNTIME_SPEC.md` |
| 24 角色卡生成 | `DESIGN.md` §2.2-10（原则）；细节 `CHARACTER_CARD_SPEC.md` |
| 25 导入 = 新实例 | `WORLD_SETTING_SPEC.md` |
| 26 安卓内建聊天 | `ANDROID_SPEC.md`、`CHANNEL_PLUGIN_SPEC.md` §四 |
| 27 不支持世界合并 | `WORLD_SETTING_SPEC.md` |
| 28 倍率整秒生效与归属 | `WORLD_RUNTIME_SPEC.md` |
| 29 共享生成器与版本兼容 | `ANDROID_SPEC.md`、`WORLD_SETTING_SPEC.md` |
| 30 导出含名称 / 导入命名 | `WORLD_SETTING_SPEC.md` |
| 31 倍率上限 | `WORLD_RUNTIME_SPEC.md` |
| 32 名称嵌入快照 / 改名不生效 | `WORLD_SETTING_SPEC.md` |
| 33 全局同名约束 | `WORLD_SETTING_SPEC.md` |
| 34 用户改包不提供支持 | `WORLD_SETTING_SPEC.md` |
